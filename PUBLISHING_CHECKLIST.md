# Чеклист для публикации ComfyUI Preset Download Manager

## ✅ Структура проекта

- [x] `__init__.py` - регистрация ноды
- [x] `nodes.py` - основная логика
- [x] `web/js/preset_download_manager.js` - UI интерфейс
- [x] `requirements.txt` - зависимости
- [x] `README.md` - документация
- [x] `.gitignore` - игнорируемые файлы
- [x] `LICENSE` - лицензия MIT

## ✅ Проверка файлов

### __init__.py
- [x] `NODE_CLASS_MAPPINGS` определен
- [x] `NODE_DISPLAY_NAME_MAPPINGS` определен
- [x] `WEB_DIRECTORY = "./web"` (правильный путь)

### nodes.py
- [x] Класс `PresetDownloadManager` определен
- [x] `INPUT_TYPES`, `RETURN_TYPES`, `FUNCTION`, `CATEGORY` определены
- [x] HTTP endpoints зарегистрированы

### web/js/preset_download_manager.js
- [x] Файл существует в правильной директории
- [x] Используется `app.registerExtension`
- [x] Класс `PresetDownloadManagerNode` определен

### requirements.txt
- [x] `huggingface-hub>=0.20.0`
- [x] `aiohttp>=3.9.0`

### .gitignore
- [x] `presets.json` игнорируется (создается автоматически)
- [x] `__pycache__/` игнорируется
- [x] Модели игнорируются (*.safetensors, *.ckpt, *.pt, *.pth)

## 📝 Перед публикацией

1. **Убедитесь, что presets.json не в репозитории:**
   ```bash
   git rm --cached presets.json
   ```

2. **Проверьте, что все работает:**
   - Нода появляется в ComfyUI
   - Кнопка "Open Manager" работает
   - Модальное окно открывается
   - Загрузка моделей работает

3. **Обновите README.md:**
   - Добавьте скриншоты (если есть)
   - Убедитесь, что инструкции актуальны

4. **Создайте релиз на GitHub:**
   - Версия (например, v1.0.0)
   - Описание изменений
   - Прикрепите архив или используйте git tags

## 🚀 Публикация

### Вариант 1: GitHub репозиторий
1. Создайте репозиторий на GitHub
2. Загрузите код:
   ```bash
   git init
   git add .
   git commit -m "Initial release"
   git remote add origin <your-repo-url>
   git push -u origin main
   ```

### Вариант 2: ComfyUI Manager
Если хотите добавить в ComfyUI Manager, создайте файл `custom_nodes.json` в репозитории ComfyUI Manager с записью:
```json
{
  "custom_nodes": [
    {
      "author": "YourName",
      "title": "ComfyUI Preset Download Manager",
      "reference": "https://github.com/yourusername/ComfyUI-PresetDownloadManager",
      "files": [
        "https://github.com/yourusername/ComfyUI-PresetDownloadManager"
      ]
    }
  ]
}
```

## ⚠️ Важные замечания

- `presets.json` НЕ должен быть в репозитории (создается автоматически)
- `WEB_DIRECTORY` должен быть `"./web"`, а не `"./web/js"`
- Все зависимости должны быть в `requirements.txt`
- Убедитесь, что нет хардкода путей или конфигураций

